# Research

Материалы валидации предпосылок проекта — до проектирования детекторов.

- `step0-show-report-baseline.md` — сверка того, что `ora2pg SHOW_REPORT` уже
  показывает "из коробки" по каждому классу проблемных конструкций
  (PACKAGE/PACKAGE BODY, PRAGMA AUTONOMOUS_TRANSACTION, CONNECT BY,
  DBMS_*/UTL_*, COMPOUND TRIGGER), с реальными открытыми PL/SQL-примерами.
