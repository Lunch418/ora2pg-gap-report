# Changelog

Формат по мотивам [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).
Проект следует [SemVer](https://semver.org/lang/ru/) в упрощённом виде,
пока не достигнет 1.0.0: minor-версия — новые детекторы/возможности,
patch — исправления в существующих.

## [Unreleased]

## [0.5.0] - 2026-08-17

### Changed
- `estimate_hours()` больше не суммирует диапазон часов по каждой
  находке независимо — теперь только первое вхождение каждого детектора
  оценивается по полному диапазону severity, а каждое повторное
  вхождение того же детектора — по отдельному, плоскому диапазону
  «применить уже понятный фикс» (0.25–1 ч, не зависит от severity).
  Раньше 8 находок `autonomous_tx` в одном пакете считались как 8
  независимых high-задач (16–64 ч только на них), хотя это один
  выученный паттерн (dblink-обёртка), применённый 8 раз. На `logger.pkb`
  + `logger.pks` (28 находок) оценка изменилась с ~38–152 ч до ~11–45 ч.
  Терминальная панель «Оценка ручной доработки» теперь отдельной строкой
  показывает `N паттернов из M находок`, когда они расходятся — новая
  `distinct_detector_count()` в `effort_estimator.py`.

### Documentation
- Задокументированы четыре реальных, ранее нигде явно не описанных
  ограничения (найдены сторонним построчным ревью, каждое подтверждено
  отдельно, не принято на слово): `dbms_utl_calls._CONVERTED` — список,
  привязанный к конкретной версии `ora2pg`, требует ручного
  сопровождения при обновлении `ora2pg`, а не проверяется автоматически;
  `ora2pg_wrapper.py` парсит `--estimate_cost` по точному текстовому
  формату вывода `ora2pg`, который не является для него официальным
  стабильным интерфейсом; `oracle_connector.export_schema()` выгружает
  только `PACKAGE BODY`/`TRIGGER`, standalone-процедуры/функции, вьюхи и
  типы не выгружаются автоматически (хотя анализируются корректно, если
  их DDL скормить файлом); видимость динамического SQL
  (`mask_dynamic_sql_visible()`) не отслеживает SQL, собранный по частям
  в переменную несколькими операторами до `EXECUTE IMMEDIATE`, и не
  поддерживает старый API `DBMS_SQL.PARSE`/`EXECUTE` вообще.

### Added
- `--verify` — пост-миграционная статическая проверка: сравнивает
  pre-migration находки (снапшот `--save`) с тем, что реально осталось в
  уже сгенерированном ora2pg PostgreSQL-коде. Не функциональная/
  поведенческая проверка — никуда не подключается, ничего не выполняет,
  просто перезапускает те же детекторы на сгенерированном файле.
  Сравнение на уровне детектора, не отдельной находки (сопоставление по
  файлу/объекту/фрагменту, как в `--baseline`, не переживает границу
  Oracle→PostgreSQL: `ora2pg` переименовывает объекты, файл в любом
  случае другой). Новый модуль `verification.py`: 13 детекторов, чью
  находку `ora2pg` копирует в вывод практически без изменений
  (`cross_apply`, `json_table`, `identity_column` и другие — полный
  список в `docs/ARCHITECTURE.md`), помечены `VERBATIM` — для них
  повторный прогон детектора по выводу содержательный: `STILL_PRESENT`/
  `NOT_DETECTED`. 15 детекторов, чью находку `ora2pg` либо целиком
  отбрасывает, либо манглит окружающую структуру достаточно, чтобы
  повторному обнаружению нельзя было доверять, помечены
  `NOT_VERIFIABLE` — для них `NOT_DETECTED` был бы тавтологией
  (конструкции гарантированно не будет в выводе на любой миграции,
  вне зависимости от того, исправил ли кто-то проблему вручную), а не
  сигналом. `connect_by` не участвует — он и так анализирует только
  сгенерированный код. `--format terminal`/`--format json`; `doctor.py`
  проверяет, что каждый детектор классифицирован.

### Added
- `--lang en` / `--set-lang` / `ORA2PG_GAP_REPORT_LANG` — английский вывод
  как опция, русский остаётся языком по умолчанию (не меняется без
  явного действия — ни один существующий скрипт/CI, парсящий текущий
  русский вывод, не сломается). `--set-lang` открывает интерактивный
  выбор языка (стилизованная панель rich, `[1] English`/`[2] Русский`)
  и сохраняет его в
  `~/.config/ora2pg-gap-report/language` (или `$XDG_CONFIG_HOME`) для всех
  будущих запусков; тот же выбор показывается один раз сам при первом
  запуске в интерактивном терминале, если язык нигде ещё не задан.
  Приоритет: `--lang` → env-переменная → сохранённый выбор →
  интерактивный выбор → русский. Новый модуль `ora2pg_gap_report/i18n.py`:
  вся UI-строка терминального/markdown/HTML отчёта, все runtime-сообщения
  об ошибках и предупреждения, плюс английский перевод объяснения и
  рекомендации каждого из 29 детекторов (`EXPLANATION_EN`, ключ — сам
  русский текст находки, не имя детектора, потому что у `bulk_collect`
  три разных сообщения под одним детектором). Перевод подставляется один
  раз, централизованно, до того как находки попадают в любой из форматов
  вывода — json/csv/sarif не нуждаются в собственной логике перевода.
  `--help` и содержимое `docs/research/*.md` (в т.ч. то, что печатает
  `--explain`) остаются на русском — оба намеренно вне рамок этого
  изменения, см. `i18n.py`. `doctor.py`: новая проверка — каждая
  константа-сообщение детектора должна иметь перевод в `EXPLANATION_EN`
  (иначе `--lang en` тихо откатится на русский для этой находки, ничего
  об этом не сообщив).

### Fixed
- Четыре висячих упоминания несуществующего `PROJECT_BRIEF.md` (`cli.py`,
  `report_generator.py`, `effort_estimator.py`, `oracle_connector.py`) —
  заменены на ссылки на реальные разделы `README.md`/`docs/ARCHITECTURE.md`
  (issue #1).

### Added
- `GapEntry` (`gap_registry.py`) получил поля `ora2pg_version`/
  `postgresql_version` — на каких версиях реально подтверждена находка
  (по умолчанию `25.0`/`16`, как и было для всех 28 до сих пор, но
  структура данных готова к разным версиям для будущих находок без
  правки всех записей разом). `--explain` теперь печатает эту версию.
  `docs/research/GAP_REGISTRY.md` получил колонку PostgreSQL (раньше
  версия PostgreSQL была только в общем примечании `AUDIT.md`, не
  по каждой строке). `doctor.py` сверяет колонки `GAP_REGISTRY.md` с
  `gap_registry.py` — тот же класс проверки, что и для файлового дерева
  детекторов, только для версий.

### Changed
- README.md (540 строк) разделён: внутренняя архитектура (лексер,
  маскирование, атрибуция, обработка динамического SQL, файловая
  структура) переехала в `docs/ARCHITECTURE.md`, тестирование/corpus/
  живой Oracle — в `docs/DEVELOPMENT.md`. README остаётся про "что это
  и зачем" — проблема, детекторы, установка, использование. `doctor.py`
  обновлён вслед за переездом: проверка file tree детекторов теперь
  сверяет `docs/ARCHITECTURE.md` с диском, не README.md.

### Added
- `doctor.py`: новая проверка — файловое дерево детекторов в README.md
  («Архитектура») сверяется со списком реальных файлов в
  `ora2pg_gap_report/detectors/` в обе стороны (лишняя запись/пропущенный
  файл — обе ошибки). Закрывает ровно тот класс проблемы, из-за которого
  README до этого релиза какое-то время утверждал «три из четырёх
  детекторов», хотя их уже 29.
- В README задокументированы критерии `severity` (26 из 28
  зарегистрированных gap'ов — `high`, `dbms_utl_calls` — 29-й детектор,
  не привязанный к конкретному GAP-NNN — тоже `medium`; не искусственное
  распределение) и добавлена
  вводная схема/quick start в самом начале файла.

- Реестр реального открытого кода расширен с четырёх проектов до семи:
  добавлены `method5/plsql_lexer` (лексер/токенизатор PL/SQL, с
  нестандартными расширениями файлов `.plsql`/`.bdy`/`.spc`),
  `mbleron/ExcelGen` (генератор Excel-файлов) и `osalvador/tePLSQL`
  (шаблонизатор с активным использованием `EXECUTE IMMEDIATE`). Свежий
  подсчёт по всем семи вместе: **247 298 строк**, 0 ложных срабатываний,
  0 падений — включая два файла, где `EXECUTE IMMEDIATE` реально строит
  код динамически, ни разу не спровоцировав ложную атрибуцию. Число
  corpus-validated детекторов выросло с 8 до 9 — впервые на реальном
  открытом коде подтверждён `with_function` (`WITH FUNCTION` внутри
  тестового набора `mbleron/ExcelGen`), закреплено регрессионным тестом
  на настоящем фрагменте. См. `docs/research/AUDIT.md`.
- `--format html` — самодостаточная HTML-страница (инлайновый CSS, без
  внешних CSS/JS/шрифтов — работает офлайн, как и остальные форматы, см.
  раздел «Установка без интернета»), для показа отчёта заказчику/
  руководству без установки чего-либо. Та же сводка (счётчики по
  severity, грубая оценка часов с тем же предупреждением
  «неоткалиброванная эвристика, не измерение», что у markdown/terminal)
  и та же таблица находок, что у остальных форматов, с цветовой
  индикацией severity. Всё динамическое содержимое (пути файлов, имена
  объектов, сообщения) экранируется через `html.escape()`.
- Видимость конструкций, спрятанных внутри `EXECUTE IMMEDIATE` (одиночный
  строковый литерал или конкатенация `'...' || выражение || '...'`).
  14 детекторов, использующих общий индекс атрибуции (`bulk_collect`,
  `connect_by_nocycle`, `cross_apply`, `database_link`, `flashback_query`,
  `insert_all`, `json_table`, `merge_delete_clause`, `model_clause`,
  `oracle_text`, `pivot_clause`, `recursive_with`, `sql_macro`,
  `with_function`), и отдельно `autonomous_tx` (свой собственный механизм
  отслеживания границ процедур, не общий индекс) теперь используют новую
  функцию `plsql_lex.mask_dynamic_sql_visible()` — второй, отдельный вид
  маскировки, в котором аргумент `EXECUTE IMMEDIATE` остаётся видимым
  вместо замены на пробелы. Индекс "какой объект окружает эту позицию"
  при этом всегда строится из безопасного, полностью замаскированного
  текста — иначе пакет/процедура, которую код создаёт динамически в
  момент выполнения, была бы принята за настоящий объект, объявленный в
  дереве исходников, и испортила бы атрибуцию не связанных с ней находок.
  Подтверждено на реальном открытом коде (`utPLSQL`): найдены и верно
  приписаны реальной, находимой в дереве исходников процедуре — скрытая
  `PRAGMA AUTONOMOUS_TRANSACTION` (внутри динамически создаваемого
  пакета, `run_helper.pkb`) и скрытый `BULK COLLECT INTO` (внутри
  динамически выполняемого анонимного блока, `coverage_helper.pkb`) — оба
  закреплены регрессионными тестами на настоящих фрагментах. Не
  затронуто: детекторы схемного уровня (`table_partitioning`,
  `external_table`, `invisible_column` и т.д., включая часть
  `oracle_text`, отвечающую за `CREATE INDEX ... INDEXTYPE`) по-прежнему
  не видят одноимённую DDL-конструкцию, если она построена динамически —
  честно вне рамок этого изменения, см. README.md, раздел «Конструкции,
  спрятанные в динамическом SQL».
- `--save PATH` / `--baseline PATH` — снимок находок текущего прогона и
  сравнение со следующим (NEW/RESOLVED/UNCHANGED), для отслеживания
  прогресса миграции между прогонами. Находки сопоставляются по
  стабильному отпечатку (детектор + файл + объект + фрагмент), не по
  номеру строки. Оба флага всегда работают по полному набору находок,
  независимо от `--severity`/`--object`.
- `--fail-on high|medium|low` — код возврата `1`, если среди находок есть
  хотя бы одна с этим уровнем серьёзности или выше — для CI-гейта. Тоже
  оценивается по полному набору находок, а не по тому, что осталось
  после `--severity`/`--object`.
- `--explain GAP-NNN` — печатает research-документ конкретного gap'а
  прямо в терминал, без сканирования файлов. В pip-пакете (где
  `docs/research/` не поставляется) вместо текста показывает прямую
  ссылку на документ на GitHub — подтверждено сборкой реального wheel и
  прогоном из чистого venv вне репозитория, не только юнит-тестом.
- `ora2pg_gap_report/gap_registry.py` — единый источник данных о всех 28
  gap'ах (номер, детектор, тестовые файлы), которым пользуются
  `--explain` и `scripts/audit_gap_test_counts.py` (раньше вёл
  собственный отдельный список).
- `scripts/doctor.py` — проверка целостности реестра: для каждого
  GAP-NNN есть research-документ, детектор и тесты (включая хотя бы один
  позитивный и хотя бы один guard-тест). Теперь часть CI (`lint` job) —
  расхождение реестра с файлами на диске ломает сборку сразу.
- `--format csv` — плоский вывод находок для аналитиков/электронных
  таблиц, те же поля и порядок, что у `--format json`.
- `schemas/report.schema.json` и `schemas/baseline.schema.json` —
  формальные JSON Schema для `--format json` и файлов
  `--save`/`--baseline`, проверяются в тестах против реального вывода
  инструмента (`tests/test_schemas.py`), не просто написаны вручную.
  `jsonschema` — новая dev-зависимость (только для этой проверки в
  тестах, не для самого CLI).
- `--format sarif` — SARIF 2.1.0 для GitHub code scanning / GitLab SAST.
  Severity сопоставлена с уровнями SARIF (high → error, medium → warning,
  low → note), один rule на реально встретившийся детектор с ссылкой на
  research-документ (`helpUri`), если детектор входит в реестр GAP-NNN.
  Проверяется в тестах против официальной SARIF 2.1.0 схемы OASIS
  (`tests/test_sarif.py`, схема заведена в `tests/fixtures/`).

### Fixed
- README: секция «Архитектура» описывала архитектуру версии с четырьмя
  детекторами (`autonomous_tx`/`compound_triggers`/`dbms_utl_calls`/
  `connect_by`) — устаревший текст, оставшийся с v0.1.0, хотя выше по
  тому же файлу уже перечислены все 28. Переписано честно: `connect_by`
  — единственный детектор, которому нужен установленный `ora2pg`,
  остальные 27 — чистый Python без внешних зависимостей.

### Changed
- Реестр реального открытого кода для проверки детекторов расширен с
  двух проектов до четырёх: добавлены `utPLSQL/utPLSQL` (фреймворк
  юнит-тестирования PL/SQL) и `OraOpenSource/Logger` (канонический
  логгер). Суммарно по всем четырём — **215 214 строк** (точный свежий
  подсчёт по всем четырём репозиториям вместе на момент этой проверки,
  не сумма отдельно запомненных чисел по каждому — репозитории живые и
  растут, у предыдущей оценки ~143k для первых двух не было
  зафиксированного коммита, к которому можно было бы её привязать).
  Число corpus-validated
  детекторов выросло с 5 до 8 (добавились `autonomous_tx`,
  `global_temp_table`, `collection_type`) — каждый с новым постоянным
  регрессионным тестом на реальном фрагменте. Единственная находка —
  честно документированная граница применимости: `object_name='UNKNOWN'`
  на анонимном `declare...begin...end;`-блоке (install-скрипт, не
  `DBMS_METADATA.GET_DDL`-выгрузка объекта) — не баг, ноль ложных
  срабатываний и ноль падений на всём расширенном корпусе. См.
  `docs/research/AUDIT.md`.

## [0.4.0] - 2026-08-16

### Added
- 7 новых детекторов, GAP-022…GAP-028. Реестр — теперь 28 подтверждённых
  gap'ов, см. `docs/research/GAP_REGISTRY.md`:
  - `cross_apply` — `CROSS APPLY`/`OUTER APPLY`, синтаксиса `APPLY` нет в
    PostgreSQL вообще.
  - `oracle_text` — домен-индекс `INDEXTYPE IS CTXSYS.*` (Oracle Text)
    отбрасывается целиком; вызовы `CONTAINS`/`CATSEARCH`/`MATCHES` не
    переносятся.
  - `recursive_with` — нативная рекурсивная `WITH ... AS (...)` (не через
    `CONNECT BY`) без обязательного в PostgreSQL ключевого слова
    `RECURSIVE`.
  - `invisible_index` — индекс `INVISIBLE` теряет своё скрытие от
    оптимизатора.
  - `read_only_table` — `CREATE TABLE ... READ ONLY` теряет гарантию
    неизменяемости; `INSERT` проходит там, где Oracle гарантированно
    блокирует его (`ORA-12081`).
  - `materialized_view_log` — `CREATE MATERIALIZED VIEW LOG` не
    конвертируется вообще, след только в DEBUG-логе.
  - `identity_column` — `GENERATED ... AS IDENTITY (...)` с опциями:
    настоящий баг двойных скобок в самой подстановке ora2pg (не пропуск
    конвертации, а поломанная конвертация — код падает при загрузке DDL).
- `docs/research/AUDIT.md` и `scripts/audit_gap_test_counts.py` обновлены
  под все 28 gap'ов.

### Fixed
- Системный баг статeмент-скоупинга: `DBMS_METADATA.GET_DDL` (этот же
  проект использует его для выгрузки DDL из Oracle) по умолчанию не
  ставит `;` в конце оператора, а часть детекторов ограничивала «свой»
  оператор как «до следующей `;`, а если её нет — до конца файла», из-за
  чего конструкция из более поздней таблицы могла ошибочно приписаться
  более ранней, незавершённой. Затронуло в том числе уже опубликованные
  `table_partitioning` и `external_table`, не только новые детекторы этого
  релиза. Добавлен общий хелпер `statement_end()` в `plsql_lex.py`,
  применён в 8 детекторах: `table_partitioning`, `external_table`,
  `invisible_column`, `global_temp_table`, `oracle_text`,
  `invisible_index`, `read_only_table`, `identity_column`.
- `invisible_index`/`read_only_table`: ложные срабатывания на столбце с
  тем же именем, что и модификатор (`invisible`/`read_only` как
  идентификатор столбца, в том числе в двойных кавычках) — теперь
  исключены.
- `invisible_index`/`read_only_table`/`oracle_text`: номер строки в
  находке теперь указывает на сам модификатор/секцию (`INVISIBLE`/`READ
  ONLY`/`INDEXTYPE IS ...`), а не на строку с открывающим `CREATE
  TABLE`/`CREATE INDEX` — важно для многострочного DDL.
- `materialized_view_log`: severity поднята с `medium` на `high` — тот же
  профиль риска, что у `table_partitioning`/`external_table` (конструкция
  молча пропадает без ошибки PostgreSQL, но означает реальную
  архитектурную потерю).

## [0.3.0] - 2026-08-15

### Added
- 14 новых детекторов, GAP-008…GAP-021: `pivot_clause`, `object_type`,
  `with_function`, `flashback_query`, `global_temp_table`,
  `table_partitioning`, `connect_by_nocycle`, `context_object`,
  `insert_all`, `json_table`, `external_table`, `sql_macro`,
  `invisible_column`, `collection_type`. Реестр — теперь 21 подтверждённый
  gap, см. `docs/research/GAP_REGISTRY.md`.
- Флаг `--version`.
- Поддержка директорий в качестве аргумента — рекурсивный поиск
  `.sql`/`.pks`/`.pkb` файлов, не только явный список файлов.
- `docs/research/AUDIT.md` — сводная проверка доказательной базы по
  каждому из 21 gap'а (research-документ, реальный вывод ora2pg,
  expected/actual, тесты, включая guard-тесты на ложные срабатывания) и
  `scripts/audit_gap_test_counts.py`, который эти числа пересчитывает.
- `ruff` — job линтинга в CI (с явно закреплённым набором правил, не
  implicit-default — см. `[tool.ruff.lint]` в `pyproject.toml`).

### Fixed
- Атрибуция находок (`object_name` в отчёте) для реального кода, а не
  только синтетических примеров — найдено прогоном инструмента на двух
  больших открытых PL/SQL-кодовых базах (`mortenbra/alexandria-plsql-utils`,
  `oracle-samples/db-sample-schemas`, суммарно ~143k строк):
  - `PACKAGE`-спецификация (без `BODY`) и `CREATE VIEW`/`CREATE MATERIALIZED
    VIEW` теперь распознаются как контейнеры для атрибуции — раньше
    находки внутри них уходили в `UNKNOWN`.
  - `CREATE`-конструкция внутри списка привилегий `GRANT`/`REVOKE`
    (`GRANT ..., CREATE VIEW TO oe;`) больше не принимается за настоящее
    объявление объекта.
  - Строчные комментарии SQL*Plus (`REM`/`REMARK`) теперь маскируются —
    раньше реальное объявление, которому предшествовали только такие
    комментарии, могло потерять атрибуцию.
- `table_partitioning`: секционированный индекс (`CREATE INDEX ...
  GLOBAL PARTITION BY RANGE ...`) больше не приписывается случайной
  несвязанной таблице; добавлена поддержка стратегий `REFERENCE`/`SYSTEM`.
- `insert_all`: окно поиска `INTO` после `INSERT ALL`/`FIRST` расширено —
  не пропускало находку при длинном условии `WHEN`.
- `invisible_column`: `INVISIBLE UNIQUE`/`INVISIBLE PRIMARY KEY` и другие
  inline-ограничения после модификатора теперь тоже флагуются.
- `bulk_collect`: схемный `CREATE TYPE ... EDITIONABLE ... IS TABLE OF`
  больше не дублируется как находка этого детектора.
- Директории: файл, доступный одновременно напрямую и через директорию
  (`schema/ schema/logger.pkb`), больше не учитывается дважды; расширения
  файлов (`.SQL`/`.PKB`) сопоставляются без учёта регистра.

## [0.2.0] - 2026-08-14

### Added
- GAP-002 `merge_delete_clause` — `MERGE ... DELETE WHERE`.
- GAP-003 `bulk_collect` — `TYPE ... IS TABLE OF` / `BULK COLLECT INTO` /
  `FORALL`.
- GAP-006 `database_link` — `table@dblink_name`.
- GAP-007 `model_clause` — `MODEL PARTITION BY ... MEASURES ... RULES`.
- Полностью переработанный терминальный вывод (`rich`): баннер,
  сводная панель, дерево «объекты с наибольшим числом находок»,
  секция рекомендаций по каждому сработавшему детектору, панель оценки
  трудозатрат (лучший/среднее/худший случай).
- Флаги `--severity` и `--object` для фильтрации находок в отчёте.
- `docs/research/GAP_REGISTRY.md` — формализованный реестр gap'ов с
  версией ora2pg, на которой каждый подтверждён, и статусом
  (`confirmed`/`fixed-upstream`/`wont-fix`).
- PyPI-бейдж в README.

## [0.1.0] - 2026-08-14

Первый релиз.

### Added
- GAP-001 `autonomous_tx` — `PRAGMA AUTONOMOUS_TRANSACTION`, недооценка
  стоимости в `SHOW_REPORT`/`--estimate_cost`.
- GAP-004 `compound_triggers` — `COMPOUND TRIGGER`, тихий провал
  файлового парсера ora2pg.
- GAP-005 `connect_by` — баг подстановки `LEVEL` в сгенерированном
  `WITH RECURSIVE` (опционально, `--check-connect-by`, требует ora2pg).
- `dbms_utl_calls` — классификатор конкретных вызовов `DBMS_*`/`UTL_*`.
- CLI: `--format` (terminal/markdown/json), `--output`.
- Выгрузка DDL напрямую из Oracle: `ora2pg-gap-export`.
