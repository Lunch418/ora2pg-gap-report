# Опциональная зависимость — только для src/oracle_connector.py и
# src/oracle_export.py (живая выгрузка DDL из Oracle). Всё остальное в
# проекте (детекторы, CLI анализа) в ней не нуждается.
#
# thin-режим python-oracledb — чистый Python, без установки Oracle
# Instant Client. Это важно для закрытых контуров: не нужен native-клиент
# на машине, где запускается выгрузка.
oracledb>=2.0
